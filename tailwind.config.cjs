const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
			'sans': ['Red Hat Display', 'sans-serif']
		},
		extend: {}
	},

	plugins: []
};

module.exports = config;
